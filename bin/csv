# -*- shell-script -*-                                                                                                                                                                                                                        

CMD="column -s';' -t -n"

if [[ -n "$2" ]]; then
    $CMD "$1" | head -n 1
    $CMD "$1" | grep --color "$2"
else
    $CMD "$1"
fi
